#ifndef PHYSICS
#define PHYSICS

#include "./world.hpp"

bool worldCollisionDetectionRigid(
                                    std::pair<double, double> & position,
                                    const std::pair<double, double> velocity,
                                    const std::pair<double, double> acceleration,
                                    const std::pair<double, double> hitbox
                                ) {
    
}

#endif